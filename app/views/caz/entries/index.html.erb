<table>
  <thead>
    <tr>
      <th>Title</th>
    </tr>
  </thead>
  <tbody>
    <% @entries.each do |entry| %>
      <tr>
        <td><%= link_to entry.title, edit_path(id: entry.slug) %></td>
        <td><%= entry.category_names %></td>
      </tr>
    <% end %>
   </tbody>
 </table>
